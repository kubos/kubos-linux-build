machine:
  services:
    - docker

dependencies:
  override:
    - docker pull kubostech/kubos-linux-dev:latest

test:
  override:
      - docker run -t -v $PWD:$PWD -w $PWD kubostech/kubos-linux-dev:latest $PWD/test.sh
